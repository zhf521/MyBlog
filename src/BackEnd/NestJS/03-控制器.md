---
title: 控制器
order: 3
category: 后端
tag:
    - Nest
    - 后端
---

## 什么是控制器

控制器负责处理传入的请求和向客户端返回响应。在 Nest 中，使用 `@Controller` 装饰器来定义控制器。

## 路由

### 基本使用

Nest 中的路由是指定义请求 URL 和处理函数之间的映射关系。

在 Nest 中，使用 `@Get()`、`@Post()`、`@Put()`、`@Patch()`、`@Delete()` 等装饰器来定义路由。

例如：

```typescript
// src/test/test.controller.ts

import {
    Controller,
    Get,
    Post,
    Body,
    Patch,
    Param,
    Delete,
} from '@nestjs/common';
import { TestService } from './test.service';
import { CreateTestDto } from './dto/create-test.dto';
import { UpdateTestDto } from './dto/update-test.dto';

@Controller('test')
export class TestController {
    constructor(private readonly testService: TestService) {}

    // 使用 POST 方法，通过 /test 路径来创建数据
    @Post()
    create(@Body() createTestDto: CreateTestDto) {
        return this.testService.create(createTestDto);
    }

    // 使用 GET 方法，通过 /test 路径来访问
    @Get()
    findAll() {
        return this.testService.findAll();
    }

    // 使用 GET 方法，通过 /test/:id 路径来访问，如：/test/1
    @Get(':id')
    findOne(@Param('id') id: string) {
        return this.testService.findOne(+id);
    }

    // 使用 PATCH 方法，通过 /test/:id 路径来更新数据，如：/test/1
    @Patch(':id')
    update(@Param('id') id: string, @Body() updateTestDto: UpdateTestDto) {
        return this.testService.update(+id, updateTestDto);
    }

    // 使用 DELETE 方法，通过 /test/:id 路径来删除数据，如：/test/1
    @Delete(':id')
    remove(@Param('id') id: string) {
        return this.testService.remove(+id);
    }
}
```

### 设置路由前缀（Prefix）

在 `main.ts` 中设置路由前缀：

```typescript
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';

async function bootstrap() {
    const app = await NestFactory.create(AppModule);
    // 设置路由以 /api 为前缀，但是直接通过域名访问的 / 根路由排除
    app.setGlobalPrefix('api', {
        exclude: ['/'],
    });
    await app.listen(3000);
}
bootstrap();
```

## 处理请求参数

### 查询字符串（Query String）

获取查询字符串参数：

```typescript

```

### URL 参数（URL Params）

### 请求体（Request Body）

### 请求头（Request Header）

### 请求对象（Request Object）



## URL 参数（URL Params）

-   定义：URL 路径中的参数，用于指定资源。

-   示例：`/api/user/123`，其中 `123` 是用户 ID。

-   Nest 接收：

    使用 `@Controller` 和 `@Get` 装饰器，结合 `:参数名` 路径来获取 URL 参数。

    ```typescript
    // person.controller.ts

    @Controller('api/person')
    export class PersonController {
        @Get(':id')
        urlParam(@Param('id') id: string) {
            return `接收到的数据：id=${id}`;
        }
    }
    ```

## 查询字符串（Query String）

-   定义：通过 URL 的 `?` 后跟键值对传递数据。

-   示例：`http://example.com/search?query=keyword&page=2`。

-   Nest 接收：

    使用 `@Query` 装饰器来获取查询字符串参数。

    ```typescript
    // person.controller.ts

    @Controller('api/person')
    export class PersonController {
        @Get('find')
        query(@Query('name') name: string, @Query('age') age: number) {
            return `接收到的数据：name=${name},age=${age}`;
        }
    }
    ```

## 表单数据（application/x-www-form-urlencoded）

-   定义：HTML 表单提交时的默认编码格式，数据以键值对形式出现在请求体中。

-   示例：`username=jack&password=123456`。

-   Nest 接收：

    使用 `@Body` 装饰器结合 `encodeURIComponent` 或 `query-string` 库来处理。

    ```typescript
    // person.controller.ts

    @Controller('api/person')
    export class PersonController {
        @Post()
        body(@Body() createPersonDto: CreatePersonDto) {
            return `接收到的数据：${JSON.stringify(createPersonDto)}`;
        }
    }
    ```

## 表单数据（multipart/form-data）

-   定义：用于上传文件，使用分隔符（boundary）来区分不同的表单项。

-   示例：文件上传时，每个文件和表单字段都作为请求的一个部分。

-   Nest 接收：

    使用 `@UseInterceptors` 装饰器结合 `FilesInterceptor` 来处理文件上传。

    ```typescript
    // person.controller.ts

    @Controller('api/person')
    export class PersonController {
        @Post('file')
        @UseInterceptors(
            AnyFilesInterceptor({
                dest: 'uploads/',
            })
        )
        body2(
            @Body() createPersonDto: CreatePersonDto,
            @UploadedFiles() files: Array<Express.Multer.File>
        ) {
            console.log(files);
            return `接收到的数据：${JSON.stringify(createPersonDto)}`;
        }
    }
    ```

## JSON（application/json）

-   定义：轻量级数据交换格式，易于人阅读和机器解析。

-   示例：`{"username": "jack", "password": "123456"}`。

-   Nest 接收：

    直接指定 `Content-Type` 为 `application/json`，使用 `@Body` 装饰器来接收 JSON 数据。

    ```typescript
    // person.controller.ts

    @Controller('api/person')
    export class PersonController {
        @Post()
        body(@Body() createPersonDto: CreatePersonDto) {
            return `接收到的数据：${JSON.stringify(createPersonDto)}`;
        }
    }
    ```
